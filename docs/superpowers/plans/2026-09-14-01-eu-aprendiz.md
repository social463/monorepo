# Plano — Eu Aprendiz (Fase 1)

Spec: `docs/superpowers/specs/2026-09-14-eu-aprendiz-design.md`
Branch: `feat/eu-aprendiz`

## 1. Contrato (`packages/shared`)

- [x] `src/apprentice.ts` — tipos do schema de ficha (`ApprenticeField`,
      `ApprenticeBlock`, `ApprenticeActivitySchema`), DTOs (`ApprenticeMeetingDTO`,
      `ApprenticeActivityDTO`, `ApprenticeSubmissionDTO`, `ApprenticeTrackDTO`,
      `ApprenticeWallDTO`, `ApprenticePortfolioDTO`, `ApprenticeOverviewDTO`),
      constantes (`APPRENTICE_POSITION_CATEGORY`, `APPRENTICE_MEETING_COUNT`,
      rótulos, limites) e funções puras (`meetingStatusOf`, `isMeetingUnlocked`,
      `activityProgressOf`, `npsOf`).
- [x] `src/auth.ts` — `positionCategory: string | null` no `PublicUser`.
- [x] `src/index.ts` — `export * from './apprentice'`.
- [x] teste ao lado para as funções puras.

## 2. Banco (`apps/api/prisma`)

- [x] 13 models `Apprentice*` no `schema.prisma` + enums
      (`ApprenticeActivityKind`, `ApprenticeSurveyLearned`), relações inversas em
      `User` e `Company`.
- [x] registrar todos em `TENANT_SCOPED_MODELS` (`src/lib/tenant-scope.ts`).
- [x] `pnpm db:migrate` → `*_modulo_eu_aprendiz`.
- [x] seed: turmas A/B, os 6 encontros, fichas iniciais e o contrato.

## 3. API

- [x] `src/lib/apprentice-error.ts` — `ApprenticeError` com `status`.
- [x] `src/lib/apprentice-context.ts` — `apprenticeContextOf(request)`: usuário +
      matrícula + `isFacilitator`, numa consulta.
- [x] `src/services/apprentice-service.ts` — trilha, encontro, submissão,
      mural, contrato, portfólio, pesquisa.
- [x] `src/services/apprentice-admin-service.ts` — encontros, fichas, presença,
      reposição, turmas/matrículas, overview.
- [x] `src/lib/serialize.ts` — `toApprentice*DTO`; `positionCategory` no
      `toPublicUser`.
- [x] `src/routes/apprentice.ts` (`app.authenticate`) e
      `src/routes/apprentice-admin.ts` (`+ app.requireSectorFeature('gente-gestao')`).
- [x] registrar os dois em `src/app.ts`.
- [x] testes de rota ao lado.

## 4. Web

- [x] `src/lib/apprentice-api.ts`.
- [x] `src/pages/eu-aprendiz/EuAprendizLayout.tsx` (+ `Outlet`, sub-nav).
- [x] `TrackPage`, `MeetingPage`, `WallPage`, `ContractPage`, `PortfolioPage`,
      `AdminPanelPage`.
- [x] componentes: `ActivityForm` (renderiza o schema), `MeetingSeal`
      (placeholder do selo), `SurveyCard`, `AttendanceRoll`.
- [x] `App.tsx` — guard `ApprenticeOnly` + as rotas dentro do `AppLayout`.
- [x] `components/nav-items.ts` — item `/eu-aprendiz` com flag `apprenticeOnly`
      e o filtro correspondente em `buildNavGroups`.
- [x] testes das telas principais.

## 5. Fechamento

- [x] `pnpm --filter @legends/shared test` (868 ok) e `pnpm --filter @legends/web test`
      (3020 ok; falha só o `BrandingSection`, que já falha na `main` limpa).
- [x] `pnpm --filter @legends/api test` (3513 ok, 256 arquivos) e
      `pnpm --filter @legends/api run db:seed` (idempotente).
- [x] `apps/api/test/setup.ts` — as 13 tabelas na lista de limpeza, antes do
      `user.deleteMany()`.
- [x] **Fora do escopo, mas consertado:** o `db:seed` estava quebrado na `main`
      desde que `Course.published` virou `Course.status` — o seed ainda escrevia
      o campo antigo e morria no bloco de Cursos, antes de tudo que vem depois.
- [x] `AGENTS.md` — parágrafo sobre a área e o gate por `positionCategory`.

## Fase 2 — entregue

- [x] Prisma: `ApprenticeJourney`, `ApprenticeSectorMove`, `ApprenticeTask`,
      `ApprenticeTaskItem`, enum `ApprenticeTaskColumn`, `AgentKind.APPRENTICE`,
      arquivo (S3) em material e apresentação. Duas migrations.
- [x] Shared: etapas da jornada, elegibilidade de setor, colunas e categorias do
      quadro, e as funções puras (`apprenticeStageOf`, `sectorChangeEligibility`,
      `daysUntil`, `apprenticeTenureLabel`, `isApprenticeTaskOverdue`).
- [x] API: `apprentice-journey-service`, `apprentice-task-service`,
      `lib/apprentice-prompt`, ramo `apprentice` em `buildAgentTurn`,
      `buildApprenticeMaterialKey` + rota de presign.
- [x] Web: `JourneyTab`, `TasksTab`, `AssistantTab`, upload na aba de encontros.
- [x] `components/AgentChat` — extraído da tela do Benchmarking, que migrou junto
      (6/6 no teste dela). Uma implementação de chat, dois agentes.
- [x] Testes: 13 de rota (`apprentice-management.test.ts`) e 8 de função pura.
- [x] Correção de UI na trilha: a linha do tempo estava 12px abaixo do centro das
      bolas e o selo do encontro atual usava cor translúcida.
