# Legends — Plano 7: Evoluções (hardening, elegibilidade, moderação UI, filtro de mês) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Endurecer a autenticação (expiração de JWT + segredo obrigatório em produção); permitir que admins listem todos os colaboradores e ativem/desativem (elegibilidade); expor moderação de votos na UI admin; e adicionar filtro por mês ao histórico do perfil.

**Architecture:** (A) Um util `resolveJwtSecret` falha em produção sem `JWT_SECRET` e o registro do `@fastify/jwt` passa a assinar com `expiresIn: '7d'`. (B) Novas rotas admin `GET /admin/users` e `PATCH /admin/users/:id` (toggle `active`) e `GET /admin/votes` (votos recentes p/ moderação). (C) `ProfileDTO` ganha `months: string[]` e o perfil passa a filtrar votos recebidos por mês. (D) Frontend: `AdminPage` ganha seções "Colaboradores" e "Moderação"; `ProfilePage` ganha filtro de mês.

**Tech Stack:** Fastify, Prisma, Zod, Vitest; React, React Router, TanStack Query, Testing Library.

**Prerequisites:** Planos 1–6 concluídos (MVP). Postgres rodando. Banco de teste `legends_test`. Front consome `@legends/shared` só com `import type`.

---

## File Structure

```
apps/api/src/
├── lib/config.ts                  # resolveJwtSecret (NOVO)
├── lib/config.test.ts             # (NOVO)
├── app.ts                         # MODIFY: usa resolveJwtSecret + expiresIn 7d
├── routes/admin.ts                # MODIFY: + GET/PATCH /admin/users, GET /admin/votes
├── routes/admin.test.ts           # MODIFY: + testes das novas rotas
├── services/profile-service.ts    # MODIFY: months no perfil
├── services/profile-service.test.ts # MODIFY: asserção de months
├── routes/profile.ts              # MODIFY: months na resposta
└── routes/profile.test.ts         # MODIFY: asserção de months

packages/shared/src/profile.ts     # MODIFY: ProfileDTO.months

apps/web/src/pages/
├── AdminPage.tsx                  # MODIFY: seções Colaboradores + Moderação
├── AdminPage.test.tsx             # MODIFY: mocks das novas rotas
├── ProfilePage.tsx                # MODIFY: filtro de mês
└── ProfilePage.test.tsx           # MODIFY: mock com months + teste de filtro
```

---

## Task 1: Hardening de autenticação (JWT expiry + segredo obrigatório em produção)

**Files:**
- Create: `apps/api/src/lib/config.ts`
- Test: `apps/api/src/lib/config.test.ts`
- Modify: `apps/api/src/app.ts`

- [ ] **Step 1: Escrever o teste que falha — `apps/api/src/lib/config.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import { resolveJwtSecret } from './config'

describe('resolveJwtSecret', () => {
  it('returns the provided secret when set', () => {
    expect(resolveJwtSecret({ JWT_SECRET: 'abc', NODE_ENV: 'production' })).toBe('abc')
  })

  it('throws in production when the secret is missing', () => {
    expect(() => resolveJwtSecret({ NODE_ENV: 'production' })).toThrow()
  })

  it('falls back to a dev secret outside production', () => {
    expect(resolveJwtSecret({ NODE_ENV: 'development' })).toBe('dev-secret-change-me')
    expect(resolveJwtSecret({})).toBe('dev-secret-change-me')
  })
})
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `pnpm --filter @legends/api test src/lib/config.test.ts`
Expected: FAIL — não resolve `./config`.

- [ ] **Step 3: Implementar `apps/api/src/lib/config.ts`**

```ts
export function resolveJwtSecret(env: { NODE_ENV?: string; JWT_SECRET?: string }): string {
  if (env.JWT_SECRET) {
    return env.JWT_SECRET
  }
  if (env.NODE_ENV === 'production') {
    throw new Error('JWT_SECRET é obrigatório em produção')
  }
  return 'dev-secret-change-me'
}
```

- [ ] **Step 4: Rodar e verificar que passa**

Run: `pnpm --filter @legends/api test src/lib/config.test.ts`
Expected: PASS — 3 testes verdes.

- [ ] **Step 5: Atualizar `apps/api/src/app.ts`** — importar `resolveJwtSecret` e usar no registro do jwt com `expiresIn`. Conteúdo final completo:

```ts
import Fastify, { type FastifyInstance } from 'fastify'
import cors from '@fastify/cors'
import jwt from '@fastify/jwt'
import { resolveJwtSecret } from './lib/config'
import { authRoutes } from './routes/auth'
import { userRoutes } from './routes/users'
import { categoryRoutes } from './routes/categories'
import { periodRoutes } from './routes/periods'
import { voteRoutes } from './routes/votes'
import { badgeRoutes } from './routes/badges'
import { profileRoutes } from './routes/profile'
import { adminRoutes } from './routes/admin'

export function buildApp(): FastifyInstance {
  const app = Fastify({ logger: false })

  app.register(cors, { origin: true })
  app.register(jwt, { secret: resolveJwtSecret(process.env), sign: { expiresIn: '7d' } })

  app.decorate('authenticate', async function (request, reply) {
    try {
      await request.jwtVerify()
    } catch {
      return reply.code(401).send({ message: 'Não autorizado' })
    }
  })

  app.decorate('requireAdmin', async function (request, reply) {
    if (request.user.role !== 'ADMIN') {
      return reply.code(403).send({ message: 'Acesso restrito a administradores' })
    }
  })

  app.get('/health', async () => ({ status: 'ok' }))

  app.register(authRoutes, { prefix: '/auth' })
  app.register(userRoutes)
  app.register(categoryRoutes)
  app.register(periodRoutes)
  app.register(voteRoutes)
  app.register(badgeRoutes)
  app.register(profileRoutes)
  app.register(adminRoutes)

  return app
}
```

- [ ] **Step 6: Rodar a suíte do backend (tokens com expiry seguem válidos)**

Run: `pnpm --filter @legends/api test`
Expected: PASS — todas as specs verdes (o env de teste define `JWT_SECRET`, então `resolveJwtSecret` retorna ele).

- [ ] **Step 7: Commit**

```bash
git add apps/api/src/lib/config.ts apps/api/src/lib/config.test.ts apps/api/src/app.ts
git commit -m "feat(api): harden auth (jwt expiry + required secret in production)"
```

---

## Task 2: Rotas admin de colaboradores e moderação (TDD)

**Files:**
- Modify: `apps/api/src/routes/admin.ts`
- Modify: `apps/api/src/routes/admin.test.ts`

- [ ] **Step 1: Adicionar os testes que falham ao final do `describe('admin routes', ...)` em `apps/api/src/routes/admin.test.ts`** — inserir estes casos (antes do fechamento `})` do describe):

```ts
  it('lists all users including inactive (GET /admin/users)', async () => {
    const app = buildApp()
    await app.ready()
    const token = await adminToken(app)
    await prisma.user.create({ data: { name: 'Inativo', email: 'inativo@empresa.com', passwordHash: 'x', active: false } })
    const res = await app.inject({ method: 'GET', url: '/admin/users', headers: { authorization: `Bearer ${token}` } })
    expect(res.statusCode).toBe(200)
    // admin (criado no adminToken) + inativo
    expect(res.json().users.length).toBeGreaterThanOrEqual(2)
    await app.close()
  })

  it('toggles a user active flag (PATCH /admin/users/:id)', async () => {
    const app = buildApp()
    await app.ready()
    const token = await adminToken(app)
    const user = await prisma.user.create({ data: { name: 'Alvo', email: 'alvo@empresa.com', passwordHash: 'x' } })
    const res = await app.inject({
      method: 'PATCH',
      url: `/admin/users/${user.id}`,
      headers: { authorization: `Bearer ${token}` },
      payload: { active: false },
    })
    expect(res.statusCode).toBe(200)
    expect(res.json().user.active).toBe(false)
    await app.close()
  })

  it('returns 404 toggling an unknown user', async () => {
    const app = buildApp()
    await app.ready()
    const token = await adminToken(app)
    const res = await app.inject({
      method: 'PATCH',
      url: '/admin/users/unknown',
      headers: { authorization: `Bearer ${token}` },
      payload: { active: false },
    })
    expect(res.statusCode).toBe(404)
    await app.close()
  })

  it('forbids a non-admin from listing users (403)', async () => {
    const app = buildApp()
    await app.ready()
    const token = await devToken(app)
    const res = await app.inject({ method: 'GET', url: '/admin/users', headers: { authorization: `Bearer ${token}` } })
    expect(res.statusCode).toBe(403)
    await app.close()
  })

  it('lists recent votes for moderation (GET /admin/votes)', async () => {
    const app = buildApp()
    await app.ready()
    const token = await adminToken(app)
    const voter = await prisma.user.create({ data: { name: 'V', email: 'v@empresa.com', passwordHash: 'x' } })
    const voted = await prisma.user.create({ data: { name: 'W', email: 'w@empresa.com', passwordHash: 'x' } })
    const cat = await prisma.category.create({ data: { name: 'Colaboração', slug: 'colaboracao' } })
    const period = await prisma.votingPeriod.create({ data: { monthRef: '2026-06', startsAt: new Date('2026-06-01'), endsAt: new Date('2026-06-30'), status: 'OPEN' } })
    await prisma.vote.create({ data: { voterId: voter.id, votedId: voted.id, categoryId: cat.id, periodId: period.id, justification: 'conteúdo qualquer' } })
    const res = await app.inject({ method: 'GET', url: '/admin/votes', headers: { authorization: `Bearer ${token}` } })
    expect(res.statusCode).toBe(200)
    expect(res.json().votes).toHaveLength(1)
    expect(res.json().votes[0].voter.name).toBe('V')
    await app.close()
  })
```

- [ ] **Step 2: Rodar e verificar que falham**

Run: `pnpm --filter @legends/api test src/routes/admin.test.ts`
Expected: FAIL — as novas rotas (`/admin/users`, `/admin/votes`) ainda não existem (404/erro).

- [ ] **Step 3: Implementar as rotas em `apps/api/src/routes/admin.ts`** — conteúdo final completo do arquivo:

```ts
import type { FastifyInstance } from 'fastify'
import { Prisma } from '@prisma/client'
import { z } from 'zod'
import { prisma } from '../lib/prisma'
import { slugify } from '../lib/slug'
import { closeVotingPeriod, openVotingPeriod } from '../services/admin-service'
import { voteInclude } from '../services/voting-service'
import { toCategoryDTO, toPeriodDTO, toPublicUser, toVoteDTO } from '../lib/serialize'

const createCategorySchema = z.object({
  name: z.string().min(1),
  description: z.string().optional(),
})
const updateCategorySchema = z.object({
  name: z.string().min(1).optional(),
  description: z.string().optional(),
  active: z.boolean().optional(),
})
const openPeriodSchema = z.object({
  monthRef: z.string().regex(/^\d{4}-\d{2}$/).optional(),
})
const updateUserSchema = z.object({
  active: z.boolean(),
})

export async function adminRoutes(app: FastifyInstance) {
  const adminOnly = { onRequest: [app.authenticate, app.requireAdmin] }

  app.get('/admin/categories', adminOnly, async (_request, reply) => {
    const categories = await prisma.category.findMany({ orderBy: { name: 'asc' } })
    return reply.send({ categories: categories.map(toCategoryDTO) })
  })

  app.post('/admin/categories', adminOnly, async (request, reply) => {
    const parsed = createCategorySchema.safeParse(request.body)
    if (!parsed.success) {
      return reply.code(400).send({ message: 'Dados inválidos' })
    }
    try {
      const category = await prisma.category.create({
        data: { name: parsed.data.name, slug: slugify(parsed.data.name), description: parsed.data.description },
      })
      return reply.code(201).send({ category: toCategoryDTO(category) })
    } catch (err) {
      if (err instanceof Prisma.PrismaClientKnownRequestError && err.code === 'P2002') {
        return reply.code(409).send({ message: 'Já existe uma categoria com esse nome.' })
      }
      throw err
    }
  })

  app.patch('/admin/categories/:id', adminOnly, async (request, reply) => {
    const { id } = request.params as { id: string }
    const parsed = updateCategorySchema.safeParse(request.body)
    if (!parsed.success) {
      return reply.code(400).send({ message: 'Dados inválidos' })
    }
    try {
      const category = await prisma.category.update({ where: { id }, data: parsed.data })
      return reply.send({ category: toCategoryDTO(category) })
    } catch (err) {
      if (err instanceof Prisma.PrismaClientKnownRequestError && err.code === 'P2025') {
        return reply.code(404).send({ message: 'Categoria não encontrada' })
      }
      throw err
    }
  })

  app.get('/admin/users', adminOnly, async (_request, reply) => {
    const users = await prisma.user.findMany({ orderBy: { name: 'asc' } })
    return reply.send({ users: users.map(toPublicUser) })
  })

  app.patch('/admin/users/:id', adminOnly, async (request, reply) => {
    const { id } = request.params as { id: string }
    const parsed = updateUserSchema.safeParse(request.body)
    if (!parsed.success) {
      return reply.code(400).send({ message: 'Dados inválidos' })
    }
    try {
      const user = await prisma.user.update({ where: { id }, data: { active: parsed.data.active } })
      return reply.send({ user: toPublicUser(user) })
    } catch (err) {
      if (err instanceof Prisma.PrismaClientKnownRequestError && err.code === 'P2025') {
        return reply.code(404).send({ message: 'Usuário não encontrado' })
      }
      throw err
    }
  })

  app.post('/admin/periods', adminOnly, async (request, reply) => {
    const parsed = openPeriodSchema.safeParse(request.body ?? {})
    if (!parsed.success) {
      return reply.code(400).send({ message: 'monthRef inválido (use YYYY-MM)' })
    }
    try {
      const period = await openVotingPeriod(parsed.data.monthRef)
      return reply.code(201).send({ period: toPeriodDTO(period) })
    } catch (err) {
      if (err instanceof Prisma.PrismaClientKnownRequestError && err.code === 'P2002') {
        return reply.code(409).send({ message: 'Já existe um período para esse mês.' })
      }
      throw err
    }
  })

  app.post('/admin/periods/:id/close', adminOnly, async (request, reply) => {
    const { id } = request.params as { id: string }
    try {
      const period = await closeVotingPeriod(id)
      return reply.send({ period: toPeriodDTO(period) })
    } catch (err) {
      if (err instanceof Prisma.PrismaClientKnownRequestError && err.code === 'P2025') {
        return reply.code(404).send({ message: 'Período não encontrado' })
      }
      throw err
    }
  })

  app.get('/admin/votes', adminOnly, async (_request, reply) => {
    const votes = await prisma.vote.findMany({ include: voteInclude, orderBy: { createdAt: 'desc' }, take: 50 })
    return reply.send({ votes: votes.map(toVoteDTO) })
  })

  app.delete('/admin/votes/:id', adminOnly, async (request, reply) => {
    const { id } = request.params as { id: string }
    try {
      await prisma.vote.delete({ where: { id } })
      return reply.code(204).send()
    } catch (err) {
      if (err instanceof Prisma.PrismaClientKnownRequestError && err.code === 'P2025') {
        return reply.code(404).send({ message: 'Voto não encontrado' })
      }
      throw err
    }
  })
}
```

- [ ] **Step 4: Rodar e verificar que passam**

Run: `pnpm --filter @legends/api test src/routes/admin.test.ts`
Expected: PASS — os 8 testes anteriores + 5 novos = 13 verdes.

- [ ] **Step 5: Commit**

```bash
git add apps/api/src/routes/admin.ts apps/api/src/routes/admin.test.ts
git commit -m "feat(api): admin user eligibility toggle and votes listing for moderation"
```

---

## Task 3: Meses no perfil (`ProfileDTO.months`) (TDD)

**Files:**
- Modify: `packages/shared/src/profile.ts`
- Modify: `apps/api/src/services/profile-service.ts`
- Modify: `apps/api/src/services/profile-service.test.ts`
- Modify: `apps/api/src/routes/profile.ts`
- Modify: `apps/api/src/routes/profile.test.ts`

- [ ] **Step 1: Adicionar `months` ao `ProfileDTO` em `packages/shared/src/profile.ts`** — bloco final do `ProfileDTO`:

```ts
export interface ProfileDTO {
  user: PublicUser
  stats: ProfileStats
  categoryBreakdown: CategoryBreakdownItem[]
  months: string[]
  badges: AwardedBadgeDTO[]
}
```

- [ ] **Step 2: Adicionar a asserção de `months` ao teste — `apps/api/src/services/profile-service.test.ts`** — no primeiro caso ("aggregates totals…"), logo após a asserção de `categoryBreakdown`, adicionar:

```ts
    expect(profile!.months).toEqual(['2026-06', '2026-05'])
```

- [ ] **Step 3: Rodar e verificar que falha**

Run: `pnpm --filter @legends/api test src/services/profile-service.test.ts`
Expected: FAIL — `profile.months` é `undefined`.

- [ ] **Step 4: Atualizar `apps/api/src/services/profile-service.ts`** — conteúdo final completo:

```ts
import type { User } from '@prisma/client'
import { prisma } from '../lib/prisma'
import { voteInclude, type VoteWithRelations } from './voting-service'

export interface CategoryCount {
  categorySlug: string
  categoryName: string
  count: number
}

export interface UserProfile {
  user: User
  totalVotesReceived: number
  monthsRecognized: number
  months: string[]
  categoryBreakdown: CategoryCount[]
}

export async function getUserProfile(userId: string): Promise<UserProfile | null> {
  const user = await prisma.user.findUnique({ where: { id: userId } })
  if (!user) return null

  const totalVotesReceived = await prisma.vote.count({ where: { votedId: userId } })

  const monthRows = await prisma.vote.findMany({
    where: { votedId: userId },
    distinct: ['periodId'],
    select: { period: { select: { monthRef: true } } },
  })
  const months = monthRows.map((row) => row.period.monthRef).sort((a, b) => b.localeCompare(a))

  const grouped = await prisma.vote.groupBy({
    by: ['categoryId'],
    where: { votedId: userId },
    _count: { _all: true },
  })
  const categories = await prisma.category.findMany({
    where: { id: { in: grouped.map((g) => g.categoryId) } },
  })
  const categoryById = new Map(categories.map((category) => [category.id, category]))
  const categoryBreakdown: CategoryCount[] = grouped
    .map((g) => {
      const category = categoryById.get(g.categoryId)
      return {
        categorySlug: category?.slug ?? g.categoryId,
        categoryName: category?.name ?? g.categoryId,
        count: g._count._all,
      }
    })
    .sort((a, b) => b.count - a.count || a.categoryName.localeCompare(b.categoryName))

  return {
    user,
    totalVotesReceived,
    monthsRecognized: months.length,
    months,
    categoryBreakdown,
  }
}

interface VoteFilters {
  month?: string
  categorySlug?: string
}

export function listVotesReceived(userId: string, filters: VoteFilters): Promise<VoteWithRelations[]> {
  return prisma.vote.findMany({
    where: {
      votedId: userId,
      ...(filters.month ? { period: { monthRef: filters.month } } : {}),
      ...(filters.categorySlug ? { category: { slug: filters.categorySlug } } : {}),
    },
    include: voteInclude,
    orderBy: { createdAt: 'desc' },
  })
}
```

- [ ] **Step 5: Rodar e verificar que passa**

Run: `pnpm --filter @legends/api test src/services/profile-service.test.ts`
Expected: PASS — 5 testes verdes (incl. a asserção de `months`).

- [ ] **Step 6: Incluir `months` na resposta de `apps/api/src/routes/profile.ts`** — no handler de `/users/:id/profile`, o `reply.send` final:

```ts
    return reply.send({
      user: toPublicUser(profile.user),
      stats: {
        totalVotesReceived: profile.totalVotesReceived,
        monthsRecognized: profile.monthsRecognized,
      },
      categoryBreakdown: profile.categoryBreakdown,
      months: profile.months,
      badges: badges.map(toAwardedBadgeDTO),
    })
```

- [ ] **Step 7: Adicionar a asserção de `months` ao teste — `apps/api/src/routes/profile.test.ts`** — no caso "returns the aggregated profile…", após a asserção de `categoryBreakdown[0]`, adicionar:

```ts
    expect(body.months).toEqual(['2026-06'])
```

- [ ] **Step 8: Rodar e verificar + suíte completa do backend**

Run: `pnpm --filter @legends/api test`
Expected: PASS — todas as specs verdes (profile-service, profile routes com `months`, e as demais).

- [ ] **Step 9: Commit**

```bash
git add packages/shared/src/profile.ts apps/api/src/services/profile-service.ts apps/api/src/services/profile-service.test.ts apps/api/src/routes/profile.ts apps/api/src/routes/profile.test.ts
git commit -m "feat: expose recognized months in profile for month filtering"
```

---

## Task 4: Frontend — seções Colaboradores e Moderação na `AdminPage` (TDD)

**Files:**
- Modify: `apps/web/src/pages/AdminPage.tsx`
- Modify: `apps/web/src/pages/AdminPage.test.tsx`

- [ ] **Step 1: Substituir `apps/web/src/pages/AdminPage.test.tsx`** (conteúdo final — cobre categorias, período, colaboradores e moderação)

```tsx
import { render, screen, fireEvent, waitFor } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { vi, type Mock } from 'vitest'
import { AdminPage } from './AdminPage'
import { apiFetch } from '../lib/api'

vi.mock('../auth/AuthContext', () => ({
  useAuth: () => ({ user: { id: 'a1', name: 'Admin', role: 'ADMIN' }, loading: false, login: vi.fn(), logout: vi.fn() }),
}))

vi.mock('../lib/api', async (importOriginal) => {
  const actual = await importOriginal<typeof import('../lib/api')>()
  return { ...actual, apiFetch: vi.fn() }
})

const mockApiFetch = apiFetch as unknown as Mock

function setupFetch() {
  mockApiFetch.mockImplementation((path: string, options?: RequestInit) => {
    const method = options?.method
    if (path === '/admin/categories' && !method) {
      return Promise.resolve({
        categories: [{ id: 'c1', name: 'Colaboração', slug: 'colaboracao', description: null, isSpecial: false, active: true }],
      })
    }
    if (path === '/admin/categories' && method === 'POST') {
      return Promise.resolve({ category: { id: 'c2', name: 'Inovação', slug: 'inovacao', description: null, isSpecial: false, active: true } })
    }
    if (path === '/periods/current') {
      return Promise.resolve({ period: { id: 'p1', monthRef: '2026-06', startsAt: '2026-06-01T00:00:00.000Z', endsAt: '2026-06-30T23:59:59.000Z', status: 'OPEN' } })
    }
    if (path === '/admin/users') {
      return Promise.resolve({
        users: [{ id: 'u9', name: 'Diego Reis', email: 'd@e.com', role: 'DEV', position: 'SRE', squad: 'Plataforma', photoUrl: null, active: true, joinedAt: '2026-01-01T00:00:00.000Z' }],
      })
    }
    if (path === '/admin/votes' && !method) {
      return Promise.resolve({
        votes: [{ id: 'v1', voter: { id: 'u2', name: 'Carla' }, voted: { id: 'u3', name: 'Bruno' }, category: { id: 'c1', name: 'Colaboração', slug: 'colaboracao' }, justification: 'texto a moderar', createdAt: '2026-06-02T00:00:00.000Z', periodId: 'p1', monthRef: '2026-06' }],
      })
    }
    if (path === '/admin/votes/v1' && method === 'DELETE') {
      return Promise.resolve({})
    }
    return Promise.reject(new Error(`unexpected ${path} ${method ?? ''}`))
  })
}

function renderPage() {
  const client = new QueryClient({ defaultOptions: { queries: { retry: false } } })
  return render(
    <QueryClientProvider client={client}>
      <MemoryRouter>
        <AdminPage />
      </MemoryRouter>
    </QueryClientProvider>,
  )
}

describe('AdminPage', () => {
  beforeEach(() => {
    vi.clearAllMocks()
    setupFetch()
  })

  it('shows categories, period, collaborators and moderation items', async () => {
    renderPage()
    expect(await screen.findByText('Colaboração')).toBeInTheDocument()
    expect(screen.getByText('2026-06')).toBeInTheDocument()
    expect(await screen.findByText('Diego Reis')).toBeInTheDocument()
    expect(await screen.findByText(/texto a moderar/)).toBeInTheDocument()
  })

  it('creates a new category', async () => {
    renderPage()
    await screen.findByText('Colaboração')
    fireEvent.change(screen.getByLabelText(/nova categoria/i), { target: { value: 'Inovação' } })
    fireEvent.click(screen.getByRole('button', { name: /adicionar/i }))
    await waitFor(() =>
      expect(mockApiFetch).toHaveBeenCalledWith('/admin/categories', expect.objectContaining({ method: 'POST' })),
    )
  })

  it('removes a vote in moderation', async () => {
    renderPage()
    await screen.findByText(/texto a moderar/)
    fireEvent.click(screen.getByRole('button', { name: /remover/i }))
    await waitFor(() =>
      expect(mockApiFetch).toHaveBeenCalledWith('/admin/votes/v1', expect.objectContaining({ method: 'DELETE' })),
    )
  })
})
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `pnpm --filter @legends/web test src/pages/AdminPage.test.tsx`
Expected: FAIL — a AdminPage atual não renderiza colaboradores/moderação (não encontra "Diego Reis" / "texto a moderar" / botão "Remover").

- [ ] **Step 3: Substituir `apps/web/src/pages/AdminPage.tsx`** (conteúdo final — adiciona Colaboradores e Moderação)

```tsx
import { useState, type FormEvent } from 'react'
import { Link } from 'react-router-dom'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import type { CategoryDTO, PublicUser, VoteDTO, VotingPeriodDTO } from '@legends/shared'
import { ApiError, apiFetch } from '../lib/api'
import { useAuth } from '../auth/AuthContext'

export function AdminPage() {
  const { user } = useAuth()
  const queryClient = useQueryClient()
  const [name, setName] = useState('')
  const [error, setError] = useState<string | null>(null)

  const isAdmin = user?.role === 'ADMIN'

  const categoriesQuery = useQuery({
    queryKey: ['admin', 'categories'],
    queryFn: () => apiFetch<{ categories: CategoryDTO[] }>('/admin/categories'),
    enabled: isAdmin,
  })
  const periodQuery = useQuery({
    queryKey: ['period'],
    queryFn: () => apiFetch<{ period: VotingPeriodDTO | null }>('/periods/current'),
    enabled: isAdmin,
  })
  const usersQuery = useQuery({
    queryKey: ['admin', 'users'],
    queryFn: () => apiFetch<{ users: PublicUser[] }>('/admin/users'),
    enabled: isAdmin,
  })
  const votesQuery = useQuery({
    queryKey: ['admin', 'votes'],
    queryFn: () => apiFetch<{ votes: VoteDTO[] }>('/admin/votes'),
    enabled: isAdmin,
  })

  const createCategory = useMutation({
    mutationFn: (body: { name: string }) =>
      apiFetch<{ category: CategoryDTO }>('/admin/categories', { method: 'POST', body: JSON.stringify(body) }),
    onSuccess: () => {
      setName('')
      setError(null)
      queryClient.invalidateQueries({ queryKey: ['admin', 'categories'] })
    },
    onError: (err) => setError(err instanceof ApiError ? err.message : 'Erro ao criar categoria.'),
  })
  const toggleCategory = useMutation({
    mutationFn: (vars: { id: string; active: boolean }) =>
      apiFetch<{ category: CategoryDTO }>(`/admin/categories/${vars.id}`, { method: 'PATCH', body: JSON.stringify({ active: vars.active }) }),
    onSuccess: () => queryClient.invalidateQueries({ queryKey: ['admin', 'categories'] }),
  })
  const openPeriod = useMutation({
    mutationFn: () => apiFetch<{ period: VotingPeriodDTO }>('/admin/periods', { method: 'POST', body: JSON.stringify({}) }),
    onSuccess: () => queryClient.invalidateQueries({ queryKey: ['period'] }),
    onError: (err) => setError(err instanceof ApiError ? err.message : 'Erro ao abrir período.'),
  })
  const closePeriod = useMutation({
    mutationFn: (id: string) => apiFetch<{ period: VotingPeriodDTO }>(`/admin/periods/${id}/close`, { method: 'POST' }),
    onSuccess: () => queryClient.invalidateQueries({ queryKey: ['period'] }),
  })
  const toggleUser = useMutation({
    mutationFn: (vars: { id: string; active: boolean }) =>
      apiFetch<{ user: PublicUser }>(`/admin/users/${vars.id}`, { method: 'PATCH', body: JSON.stringify({ active: vars.active }) }),
    onSuccess: () => queryClient.invalidateQueries({ queryKey: ['admin', 'users'] }),
  })
  const removeVote = useMutation({
    mutationFn: (id: string) => apiFetch<unknown>(`/admin/votes/${id}`, { method: 'DELETE' }),
    onSuccess: () => queryClient.invalidateQueries({ queryKey: ['admin', 'votes'] }),
  })

  if (!isAdmin) {
    return <p className="p-8 text-red-400">Acesso restrito a administradores.</p>
  }

  function handleCreate(event: FormEvent) {
    event.preventDefault()
    if (!name.trim()) return
    createCategory.mutate({ name: name.trim() })
  }

  const period = periodQuery.data?.period ?? null

  return (
    <main className="min-h-screen p-8 flex flex-col gap-8">
      <header className="flex items-center justify-between">
        <h1 className="text-3xl font-extrabold tracking-tight">Administração</h1>
        <Link to="/" className="text-sm text-slate-400 hover:text-slate-100">
          ← Voltar
        </Link>
      </header>

      <section className="flex flex-col gap-3">
        <h2 className="font-mono text-xs uppercase tracking-widest text-secondary">Período de votação</h2>
        {period ? (
          <div className="bg-surface rounded-lg p-4 flex items-center justify-between">
            <span>
              Aberto: <span className="font-mono text-secondary">{period.monthRef}</span>
            </span>
            <button
              onClick={() => closePeriod.mutate(period.id)}
              className="text-sm rounded border border-slate-600 px-3 py-1 hover:border-primary"
            >
              Fechar período
            </button>
          </div>
        ) : (
          <div className="bg-surface rounded-lg p-4 flex items-center justify-between">
            <span className="text-slate-300">Nenhum período aberto.</span>
            <button
              onClick={() => openPeriod.mutate()}
              className="text-sm rounded bg-primary text-primary-fg px-3 py-1 font-semibold hover:opacity-90"
            >
              Abrir período do mês
            </button>
          </div>
        )}
      </section>

      <section className="flex flex-col gap-3">
        <h2 className="font-mono text-xs uppercase tracking-widest text-secondary">Categorias</h2>
        <form onSubmit={handleCreate} className="flex gap-2">
          <input
            value={name}
            onChange={(event) => setName(event.target.value)}
            aria-label="Nova categoria"
            placeholder="Nova categoria"
            className="flex-1 rounded bg-bg border border-slate-700 px-3 py-2 text-slate-100 focus:border-primary focus:outline-none"
          />
          <button type="submit" className="rounded bg-primary text-primary-fg px-4 py-2 font-semibold hover:opacity-90">
            Adicionar
          </button>
        </form>
        {error && (
          <p role="alert" className="text-sm text-red-400">
            {error}
          </p>
        )}
        <ul className="flex flex-col gap-2">
          {categoriesQuery.data?.categories.map((category) => (
            <li key={category.id} className="bg-surface rounded-lg p-3 flex items-center justify-between">
              <span className={category.active ? '' : 'text-slate-500 line-through'}>{category.name}</span>
              <button
                onClick={() => toggleCategory.mutate({ id: category.id, active: !category.active })}
                className="text-xs font-mono rounded border border-slate-600 px-2 py-1 hover:border-primary"
              >
                {category.active ? 'Desativar' : 'Ativar'}
              </button>
            </li>
          ))}
        </ul>
      </section>

      <section className="flex flex-col gap-3">
        <h2 className="font-mono text-xs uppercase tracking-widest text-secondary">Colaboradores</h2>
        <ul className="flex flex-col gap-2">
          {usersQuery.data?.users.map((member) => (
            <li key={member.id} className="bg-surface rounded-lg p-3 flex items-center justify-between">
              <span className={member.active ? '' : 'text-slate-500 line-through'}>
                {member.name} <span className="font-mono text-xs text-secondary">· {member.position ?? '—'}</span>
              </span>
              <button
                onClick={() => toggleUser.mutate({ id: member.id, active: !member.active })}
                className="text-xs font-mono rounded border border-slate-600 px-2 py-1 hover:border-primary"
              >
                {member.active ? 'Desativar' : 'Ativar'}
              </button>
            </li>
          ))}
        </ul>
      </section>

      <section className="flex flex-col gap-3">
        <h2 className="font-mono text-xs uppercase tracking-widest text-secondary">Moderação de votos</h2>
        {votesQuery.data && votesQuery.data.votes.length === 0 && (
          <p className="text-slate-400">Nenhum voto registrado.</p>
        )}
        <ul className="flex flex-col gap-2">
          {votesQuery.data?.votes.map((vote) => (
            <li key={vote.id} className="bg-surface rounded-lg p-3 flex items-start justify-between gap-4">
              <div>
                <p className="font-semibold">
                  {vote.voter.name} → {vote.voted.name}{' '}
                  <span className="font-mono text-xs text-secondary">· {vote.category.name}</span>
                </p>
                <p className="text-sm text-slate-300">{vote.justification}</p>
              </div>
              <button
                onClick={() => removeVote.mutate(vote.id)}
                className="text-xs font-mono rounded border border-red-500/50 text-red-300 px-2 py-1 hover:border-red-400 shrink-0"
              >
                Remover
              </button>
            </li>
          ))}
        </ul>
      </section>
    </main>
  )
}
```

- [ ] **Step 4: Rodar e verificar que passa**

Run: `pnpm --filter @legends/web test src/pages/AdminPage.test.tsx`
Expected: PASS — 3 testes verdes.

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/pages/AdminPage.tsx apps/web/src/pages/AdminPage.test.tsx
git commit -m "feat(web): admin collaborators eligibility and vote moderation sections"
```

---

## Task 5: Frontend — filtro de mês na `ProfilePage` (TDD)

**Files:**
- Modify: `apps/web/src/pages/ProfilePage.tsx`
- Modify: `apps/web/src/pages/ProfilePage.test.tsx`

- [ ] **Step 1: Substituir `apps/web/src/pages/ProfilePage.test.tsx`** (conteúdo final — inclui `months` no mock e testa o filtro de mês)

```tsx
import { render, screen, fireEvent, waitFor } from '@testing-library/react'
import { MemoryRouter, Route, Routes } from 'react-router-dom'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { vi, type Mock } from 'vitest'
import { ProfilePage } from './ProfilePage'
import { apiFetch } from '../lib/api'

vi.mock('../lib/api', async (importOriginal) => {
  const actual = await importOriginal<typeof import('../lib/api')>()
  return { ...actual, apiFetch: vi.fn() }
})

const mockApiFetch = apiFetch as unknown as Mock

function setupFetch() {
  mockApiFetch.mockImplementation((path: string) => {
    if (path === '/users/u1/profile') {
      return Promise.resolve({
        user: { id: 'u1', name: 'Bruno Lima', email: 'b@e.com', role: 'DEV', position: 'Backend', squad: 'Core', photoUrl: null, active: true, joinedAt: '2026-01-01T00:00:00.000Z' },
        stats: { totalVotesReceived: 3, monthsRecognized: 2 },
        categoryBreakdown: [{ categorySlug: 'colaboracao', categoryName: 'Colaboração', count: 2 }],
        months: ['2026-06', '2026-05'],
        badges: [{ id: 'ub1', awardedAt: '2026-06-01T00:00:00.000Z', badge: { id: 'b1', slug: 'conector', name: 'Conector do Time', description: 'd', kind: 'CATEGORY', iconKey: 'link', threshold: 5, categorySlug: 'colaboracao' } }],
      })
    }
    if (path.startsWith('/users/u1/votes')) {
      return Promise.resolve({
        votes: [
          { id: 'v1', voter: { id: 'u2', name: 'Carla' }, voted: { id: 'u1', name: 'Bruno Lima' }, category: { id: 'c1', name: 'Colaboração', slug: 'colaboracao' }, justification: 'Ajudou no incidente.', createdAt: '2026-06-02T00:00:00.000Z', periodId: 'p1', monthRef: '2026-06' },
        ],
      })
    }
    return Promise.reject(new Error(`unexpected ${path}`))
  })
}

function renderPage() {
  const client = new QueryClient({ defaultOptions: { queries: { retry: false } } })
  return render(
    <QueryClientProvider client={client}>
      <MemoryRouter initialEntries={['/perfil/u1']}>
        <Routes>
          <Route path="/perfil/:id" element={<ProfilePage />} />
        </Routes>
      </MemoryRouter>
    </QueryClientProvider>,
  )
}

describe('ProfilePage', () => {
  beforeEach(() => {
    vi.clearAllMocks()
    setupFetch()
  })

  it('renders the profile, stats, badges, category breakdown and received votes', async () => {
    renderPage()
    expect(await screen.findByRole('heading', { name: 'Bruno Lima' })).toBeInTheDocument()
    expect(screen.getByText('3')).toBeInTheDocument()
    expect(screen.getByText('Conector do Time')).toBeInTheDocument()
    expect(await screen.findByText(/Ajudou no incidente/)).toBeInTheDocument()
  })

  it('refetches received votes filtered by category', async () => {
    renderPage()
    await screen.findByRole('heading', { name: 'Bruno Lima' })
    fireEvent.change(screen.getByLabelText(/filtrar por categoria/i), { target: { value: 'colaboracao' } })
    await waitFor(() =>
      expect(mockApiFetch).toHaveBeenCalledWith('/users/u1/votes?categorySlug=colaboracao'),
    )
  })

  it('refetches received votes filtered by month', async () => {
    renderPage()
    await screen.findByRole('heading', { name: 'Bruno Lima' })
    fireEvent.change(screen.getByLabelText(/filtrar por mês/i), { target: { value: '2026-06' } })
    await waitFor(() =>
      expect(mockApiFetch).toHaveBeenCalledWith('/users/u1/votes?month=2026-06'),
    )
  })
})
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `pnpm --filter @legends/web test src/pages/ProfilePage.test.tsx`
Expected: FAIL — não existe o filtro "Filtrar por mês" ainda.

- [ ] **Step 3: Substituir `apps/web/src/pages/ProfilePage.tsx`** (conteúdo final — adiciona estado e select de mês; monta a query string com `URLSearchParams`)

```tsx
import { useState } from 'react'
import { Link, useParams } from 'react-router-dom'
import { useQuery } from '@tanstack/react-query'
import type { ProfileDTO, VoteDTO } from '@legends/shared'
import { apiFetch } from '../lib/api'

export function ProfilePage() {
  const { id } = useParams<{ id: string }>()
  const [categorySlug, setCategorySlug] = useState('')
  const [month, setMonth] = useState('')

  const profileQuery = useQuery({
    queryKey: ['profile', id],
    queryFn: () => apiFetch<ProfileDTO>(`/users/${id}/profile`),
    enabled: Boolean(id),
  })
  const votesQuery = useQuery({
    queryKey: ['profile-votes', id, categorySlug, month],
    queryFn: () => {
      const params = new URLSearchParams()
      if (categorySlug) params.set('categorySlug', categorySlug)
      if (month) params.set('month', month)
      const qs = params.toString()
      return apiFetch<{ votes: VoteDTO[] }>(`/users/${id}/votes${qs ? `?${qs}` : ''}`)
    },
    enabled: Boolean(id),
  })

  if (profileQuery.isLoading) {
    return <p className="p-8 font-mono text-secondary">Carregando perfil…</p>
  }
  if (profileQuery.isError || !profileQuery.data) {
    return <p className="p-8 text-red-400">Perfil não encontrado.</p>
  }

  const profile = profileQuery.data

  return (
    <main className="min-h-screen p-8 flex flex-col gap-6">
      <header className="flex items-start justify-between">
        <div>
          <h1 className="text-3xl font-extrabold tracking-tight">{profile.user.name}</h1>
          <p className="font-mono text-xs text-secondary">
            {profile.user.position ?? '—'} · {profile.user.squad ?? '—'}
          </p>
        </div>
        <Link to="/" className="text-sm text-slate-400 hover:text-slate-100">
          ← Voltar
        </Link>
      </header>

      <section className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">
        <div className="bg-surface rounded-lg p-4">
          <p className="font-mono text-xs uppercase tracking-widest text-secondary">Votos recebidos</p>
          <p className="text-2xl font-extrabold">{profile.stats.totalVotesReceived}</p>
        </div>
        <div className="bg-surface rounded-lg p-4">
          <p className="font-mono text-xs uppercase tracking-widest text-secondary">Meses reconhecido</p>
          <p className="text-2xl font-extrabold">{profile.stats.monthsRecognized}</p>
        </div>
      </section>

      <section>
        <h2 className="font-mono text-xs uppercase tracking-widest text-secondary mb-3">Selos conquistados</h2>
        {profile.badges.length === 0 ? (
          <p className="text-slate-400">Nenhum selo ainda.</p>
        ) : (
          <ul className="flex flex-wrap gap-2">
            {profile.badges.map((awarded) => (
              <li key={awarded.id} className="bg-surface border border-primary rounded-lg px-3 py-2 text-sm">
                {awarded.badge.name}
              </li>
            ))}
          </ul>
        )}
      </section>

      <section>
        <h2 className="font-mono text-xs uppercase tracking-widest text-secondary mb-3">
          Categorias mais reconhecidas
        </h2>
        {profile.categoryBreakdown.length === 0 ? (
          <p className="text-slate-400">Ainda sem reconhecimentos.</p>
        ) : (
          <ul className="flex flex-col gap-1">
            {profile.categoryBreakdown.map((item) => (
              <li key={item.categorySlug} className="flex justify-between bg-surface rounded px-3 py-2">
                <span>{item.categoryName}</span>
                <span className="font-mono text-secondary">{item.count}</span>
              </li>
            ))}
          </ul>
        )}
      </section>

      <section>
        <div className="flex items-center justify-between mb-3 flex-wrap gap-2">
          <h2 className="font-mono text-xs uppercase tracking-widest text-secondary">
            Reconhecimentos recebidos
          </h2>
          <div className="flex gap-2">
            <select
              value={month}
              onChange={(event) => setMonth(event.target.value)}
              aria-label="Filtrar por mês"
              className="rounded bg-bg border border-slate-700 px-2 py-1 text-sm text-slate-100"
            >
              <option value="">Todos os meses</option>
              {profile.months.map((monthRef) => (
                <option key={monthRef} value={monthRef}>
                  {monthRef}
                </option>
              ))}
            </select>
            <select
              value={categorySlug}
              onChange={(event) => setCategorySlug(event.target.value)}
              aria-label="Filtrar por categoria"
              className="rounded bg-bg border border-slate-700 px-2 py-1 text-sm text-slate-100"
            >
              <option value="">Todas as categorias</option>
              {profile.categoryBreakdown.map((item) => (
                <option key={item.categorySlug} value={item.categorySlug}>
                  {item.categoryName}
                </option>
              ))}
            </select>
          </div>
        </div>
        {votesQuery.data && votesQuery.data.votes.length === 0 && (
          <p className="text-slate-400">Nenhum reconhecimento nesse filtro.</p>
        )}
        <ul className="flex flex-col gap-2">
          {votesQuery.data?.votes.map((vote) => (
            <li key={vote.id} className="bg-surface rounded-lg p-4">
              <p className="font-semibold">
                {vote.category.name} <span className="font-mono text-xs text-secondary">· {vote.monthRef}</span>
              </p>
              <p className="text-sm text-slate-300">{vote.justification}</p>
              <p className="font-mono text-[11px] text-slate-500 mt-1">por {vote.voter.name}</p>
            </li>
          ))}
        </ul>
      </section>
    </main>
  )
}
```

- [ ] **Step 4: Rodar e verificar que passa**

Run: `pnpm --filter @legends/web test src/pages/ProfilePage.test.tsx`
Expected: PASS — 3 testes verdes (incl. filtro de mês).

- [ ] **Step 5: Suíte completa do frontend + build**

Run:
```bash
pnpm --filter @legends/web test
pnpm --filter @legends/web build
```
Expected: PASS em todos os specs do web; build (tsc + vite) com sucesso.

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/pages/ProfilePage.tsx apps/web/src/pages/ProfilePage.test.tsx
git commit -m "feat(web): add month filter to developer profile history"
```

---

## Self-Review (cobertura vs. evoluções)

- **Hardening de auth:** `resolveJwtSecret` (fail-fast em produção) + `expiresIn: '7d'` no jwt (Task 1). ✔
- **Elegibilidade (ativar/desativar colaborador):** `GET /admin/users` + `PATCH /admin/users/:id` + seção "Colaboradores" na AdminPage (Tasks 2, 4). ✔
- **Moderação por UI:** `GET /admin/votes` (lista) + `DELETE /admin/votes/:id` (já existia) + seção "Moderação" com botão Remover (Tasks 2, 4). ✔
- **Filtro de histórico por mês:** `ProfileDTO.months` + perfil filtra por `?month=` no front (Tasks 3, 5). ✔
- **Autorização:** novas rotas admin sob `requireAdmin` (401/403); testes de 403 inclusos (Task 2). ✔
- **Regra `@legends/shared` type-only no front:** AdminPage/ProfilePage usam `import type`. ✔
- **Type consistency:** reutiliza `PublicUser`/`VoteDTO`; `ProfileDTO.months` adicionado em Task 3 e consumido em Task 5. ✔
```
