# Legends — Plano 2: Auth & Usuários Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Autenticação por email/senha com JWT no backend (registro, login, `/me`) e listagem de devs elegíveis (`GET /users`), com tela de Login + sessão persistida no frontend.

**Architecture:** O backend ganha um cliente Prisma singleton, infra de testes com banco dedicado (`legends_test`), utilitários de senha (bcrypt), plugin `@fastify/jwt` com um decorator `authenticate`, um serviço de auth e rotas `/auth/*` e `/users` com validação Zod. O frontend ganha um cliente HTTP com token em `localStorage`, um `AuthContext`, rota protegida, página de Login e uma página de Time (lista `GET /users`) usando React Query e React Router. Tipos de DTO vivem em `@legends/shared` e são consumidos pelo front **apenas como `import type`** (apagados em runtime, sem custo de empacotamento).

**Tech Stack:** Fastify 4, `@fastify/jwt`, bcryptjs, Zod, Prisma 5/Postgres, Vitest, `pg` (criação do banco de teste); React 18, React Router 6, TanStack Query 5, Testing Library.

**Prerequisites:** Plano 1 concluído. Postgres rodando (`docker compose up -d`). Migration `init` aplicada.

---

## File Structure

```
packages/shared/src/
└── auth.ts                         # tipos PublicUser, LoginRequest, RegisterRequest, AuthResponse

apps/api/
├── package.json                    # + pg, @types/pg (devDeps)
├── vitest.config.ts                # + globalSetup, setupFiles, env de teste
├── test/
│   ├── global-setup.ts             # cria legends_test + migrate deploy
│   └── setup.ts                    # trunca tabelas antes de cada teste
└── src/
    ├── lib/
    │   ├── prisma.ts               # PrismaClient singleton
    │   ├── password.ts             # hashPassword / verifyPassword
    │   └── serialize.ts            # toPublicUser(User) -> PublicUser
    ├── types/
    │   └── fastify.d.ts            # augmentations: authenticate + JWT payload
    ├── services/
    │   └── auth-service.ts         # registerUser / authenticateUser
    ├── routes/
    │   ├── auth.ts                 # POST /auth/register, /auth/login, GET /auth/me
    │   └── users.ts                # GET /users
    └── app.ts                      # MODIFY: registra jwt, authenticate, rotas

apps/web/
├── package.json                    # + react-router-dom, @tanstack/react-query
├── vite.config.ts                  # MODIFY: proxy /api com rewrite
└── src/
    ├── lib/api.ts                  # apiFetch, token storage, ApiError
    ├── auth/
    │   ├── AuthContext.tsx         # AuthProvider + useAuth
    │   └── ProtectedRoute.tsx
    ├── pages/
    │   ├── LoginPage.tsx
    │   └── TeamPage.tsx
    └── App.tsx                     # MODIFY: providers + router
```

---

## Task 1: Tipos de auth no `@legends/shared`

**Files:**

- Create: `packages/shared/src/auth.ts`
- Modify: `packages/shared/src/index.ts`

- [ ] **Step 1: Criar `packages/shared/src/auth.ts`**

```ts
import type { UserRole } from "./enums";

export interface PublicUser {
  id: string;
  name: string;
  email: string;
  role: UserRole;
  position: string | null;
  squad: string | null;
  photoUrl: string | null;
  active: boolean;
  joinedAt: string;
}

export interface LoginRequest {
  email: string;
  password: string;
}

export interface RegisterRequest {
  name: string;
  email: string;
  password: string;
  position?: string;
  squad?: string;
}

export interface AuthResponse {
  token: string;
  user: PublicUser;
}
```

- [ ] **Step 2: Adicionar export em `packages/shared/src/index.ts`**

Conteúdo final do arquivo:

```ts
export * from "./enums";
export * from "./categories";
export * from "./auth";
```

- [ ] **Step 3: Commit**

```bash
git add packages/shared/src/auth.ts packages/shared/src/index.ts
git commit -m "feat(shared): add auth/user DTO types"
```

---

## Task 2: Prisma client singleton + infra de testes com banco dedicado

**Files:**

- Create: `apps/api/src/lib/prisma.ts`
- Create: `apps/api/test/global-setup.ts`
- Create: `apps/api/test/setup.ts`
- Modify: `apps/api/package.json` (devDeps `pg`, `@types/pg`)
- Modify: `apps/api/vitest.config.ts`

- [ ] **Step 1: Criar `apps/api/src/lib/prisma.ts`**

```ts
import { PrismaClient } from "@prisma/client";

const globalForPrisma = globalThis as unknown as { prisma?: PrismaClient };

export const prisma = globalForPrisma.prisma ?? new PrismaClient();

if (process.env.NODE_ENV !== "production") {
  globalForPrisma.prisma = prisma;
}
```

- [ ] **Step 2: Adicionar `pg` e `@types/pg` aos devDependencies de `apps/api/package.json`**

Bloco `devDependencies` final:

```json
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^20.14.15",
    "@types/pg": "^8.11.6",
    "pg": "^8.12.0",
    "prisma": "^5.18.0",
    "tsup": "^8.2.4",
    "tsx": "^4.17.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
```

- [ ] **Step 3: Instalar dependências**

Run: `pnpm install`
Expected: instala `pg` e `@types/pg` em `@legends/api`.

- [ ] **Step 4: Criar `apps/api/test/global-setup.ts`** (cria o banco de teste e aplica migrations uma vez por execução)

```ts
import { execSync } from "node:child_process";
import { Client } from "pg";

const TEST_DB = "legends_test";
const ADMIN_URL = "postgresql://legends:legends@localhost:5432/legends";
const TEST_URL = `postgresql://legends:legends@localhost:5432/${TEST_DB}?schema=public`;

export default async function setup() {
  const client = new Client({ connectionString: ADMIN_URL });
  await client.connect();
  const res = await client.query(
    "SELECT 1 FROM pg_database WHERE datname = $1",
    [TEST_DB],
  );
  if (res.rowCount === 0) {
    await client.query(`CREATE DATABASE ${TEST_DB}`);
  }
  await client.end();

  execSync("pnpm exec prisma migrate deploy", {
    stdio: "inherit",
    env: { ...process.env, DATABASE_URL: TEST_URL },
  });
}
```

- [ ] **Step 5: Criar `apps/api/test/setup.ts`** (trunca dados antes de cada teste; ordem respeita FKs)

```ts
import { afterAll, beforeEach } from "vitest";
import { prisma } from "../src/lib/prisma";

beforeEach(async () => {
  await prisma.$transaction([
    prisma.userBadge.deleteMany(),
    prisma.monthlyRankingEntry.deleteMany(),
    prisma.vote.deleteMany(),
    prisma.badge.deleteMany(),
    prisma.votingPeriod.deleteMany(),
    prisma.category.deleteMany(),
    prisma.user.deleteMany(),
  ]);
});

afterAll(async () => {
  await prisma.$disconnect();
});
```

- [ ] **Step 6: Atualizar `apps/api/vitest.config.ts`** (banco de teste via `env`, setup global e por arquivo, execução sequencial)

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    environment: "node",
    include: ["src/**/*.test.ts"],
    globalSetup: ["./test/global-setup.ts"],
    setupFiles: ["./test/setup.ts"],
    fileParallelism: false,
    env: {
      DATABASE_URL:
        "postgresql://legends:legends@localhost:5432/legends_test?schema=public",
      JWT_SECRET: "test-secret",
      NODE_ENV: "test",
    },
  },
});
```

- [ ] **Step 7: Rodar a suíte existente para validar a infra de teste**

Run: `pnpm --filter @legends/api test`
Expected: PASS — o teste `src/app.test.ts` (health) continua verde; o log mostra o `prisma migrate deploy` rodando contra `legends_test` no global-setup. (Postgres precisa estar no ar.)

- [ ] **Step 8: Commit**

```bash
git add apps/api/src/lib/prisma.ts apps/api/test apps/api/vitest.config.ts apps/api/package.json pnpm-lock.yaml
git commit -m "feat(api): add prisma singleton and test database infrastructure"
```

---

## Task 3: Utilitários de senha (TDD)

**Files:**

- Create: `apps/api/src/lib/password.ts`
- Test: `apps/api/src/lib/password.test.ts`

- [ ] **Step 1: Escrever o teste que falha — `apps/api/src/lib/password.test.ts`**

```ts
import { describe, it, expect } from "vitest";
import { hashPassword, verifyPassword } from "./password";

describe("password utils", () => {
  it("hashes a password into a different string", async () => {
    const hash = await hashPassword("changeme123");
    expect(hash).not.toBe("changeme123");
    expect(hash.length).toBeGreaterThan(20);
  });

  it("verifies a correct password", async () => {
    const hash = await hashPassword("changeme123");
    expect(await verifyPassword("changeme123", hash)).toBe(true);
  });

  it("rejects an incorrect password", async () => {
    const hash = await hashPassword("changeme123");
    expect(await verifyPassword("wrong-password", hash)).toBe(false);
  });
});
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `pnpm --filter @legends/api test src/lib/password.test.ts`
Expected: FAIL — não resolve `./password`.

- [ ] **Step 3: Implementar `apps/api/src/lib/password.ts`**

```ts
import bcrypt from "bcryptjs";

const SALT_ROUNDS = 10;

export async function hashPassword(plain: string): Promise<string> {
  return bcrypt.hash(plain, SALT_ROUNDS);
}

export async function verifyPassword(
  plain: string,
  hash: string,
): Promise<boolean> {
  return bcrypt.compare(plain, hash);
}
```

- [ ] **Step 4: Rodar e verificar que passa**

Run: `pnpm --filter @legends/api test src/lib/password.test.ts`
Expected: PASS — 3 testes verdes.

- [ ] **Step 5: Commit**

```bash
git add apps/api/src/lib/password.ts apps/api/src/lib/password.test.ts
git commit -m "feat(api): add password hashing utilities"
```

---

## Task 4: JWT + decorator `authenticate`

**Files:**

- Create: `apps/api/src/types/fastify.d.ts`
- Modify: `apps/api/src/app.ts`

- [ ] **Step 1: Criar `apps/api/src/types/fastify.d.ts`** (augmentations de tipo)

```ts
import "@fastify/jwt";
import type { FastifyReply, FastifyRequest } from "fastify";

declare module "fastify" {
  interface FastifyInstance {
    authenticate: (
      request: FastifyRequest,
      reply: FastifyReply,
    ) => Promise<void>;
  }
}

declare module "@fastify/jwt" {
  interface FastifyJWT {
    payload: { sub: string; role: string };
    user: { sub: string; role: string };
  }
}
```

- [ ] **Step 2: Substituir `apps/api/src/app.ts`** (registra jwt + decorator; as rotas são registradas nas Tasks 6 e 7 — por ora deixe os imports/registros de rota comentados conforme abaixo)

```ts
import Fastify, { type FastifyInstance } from "fastify";
import cors from "@fastify/cors";
import jwt from "@fastify/jwt";

export function buildApp(): FastifyInstance {
  const app = Fastify({ logger: false });

  app.register(cors, { origin: true });
  app.register(jwt, {
    secret: process.env.JWT_SECRET ?? "dev-secret-change-me",
  });

  app.decorate("authenticate", async function (request, reply) {
    try {
      await request.jwtVerify();
    } catch {
      return reply.code(401).send({ message: "Não autorizado" });
    }
  });

  app.get("/health", async () => ({ status: "ok" }));

  return app;
}
```

- [ ] **Step 3: Rodar a suíte para confirmar que nada quebrou**

Run: `pnpm --filter @legends/api test`
Expected: PASS — health continua verde com o jwt registrado.

- [ ] **Step 4: Commit**

```bash
git add apps/api/src/types/fastify.d.ts apps/api/src/app.ts
git commit -m "feat(api): register jwt plugin and authenticate decorator"
```

---

## Task 5: Serviço de auth (TDD)

**Files:**

- Create: `apps/api/src/lib/serialize.ts`
- Create: `apps/api/src/services/auth-service.ts`
- Test: `apps/api/src/services/auth-service.test.ts`

- [ ] **Step 1: Criar `apps/api/src/lib/serialize.ts`** (mapeia `User` do Prisma para `PublicUser`)

```ts
import type { User } from "@prisma/client";
import type { PublicUser } from "@legends/shared";

export function toPublicUser(user: User): PublicUser {
  return {
    id: user.id,
    name: user.name,
    email: user.email,
    role: user.role,
    position: user.position,
    squad: user.squad,
    photoUrl: user.photoUrl,
    active: user.active,
    joinedAt: user.joinedAt.toISOString(),
  };
}
```

- [ ] **Step 2: Escrever o teste que falha — `apps/api/src/services/auth-service.test.ts`**

```ts
import { describe, it, expect } from "vitest";
import { AuthError, authenticateUser, registerUser } from "./auth-service";

describe("auth service", () => {
  it("registers a new user with a hashed password", async () => {
    const user = await registerUser({
      name: "Ana Souza",
      email: "ana@empresa.com",
      password: "changeme123",
      position: "Backend",
      squad: "Pagamentos",
    });
    expect(user.id).toBeTruthy();
    expect(user.email).toBe("ana@empresa.com");
    expect(user.passwordHash).not.toBe("changeme123");
    expect(user.role).toBe("DEV");
  });

  it("rejects duplicate email", async () => {
    await registerUser({
      name: "Ana",
      email: "ana@empresa.com",
      password: "changeme123",
    });
    await expect(
      registerUser({
        name: "Outra Ana",
        email: "ana@empresa.com",
        password: "changeme123",
      }),
    ).rejects.toBeInstanceOf(AuthError);
  });

  it("authenticates with correct credentials", async () => {
    await registerUser({
      name: "Ana",
      email: "ana@empresa.com",
      password: "changeme123",
    });
    const user = await authenticateUser("ana@empresa.com", "changeme123");
    expect(user.email).toBe("ana@empresa.com");
  });

  it("rejects wrong password", async () => {
    await registerUser({
      name: "Ana",
      email: "ana@empresa.com",
      password: "changeme123",
    });
    await expect(
      authenticateUser("ana@empresa.com", "wrong-password"),
    ).rejects.toBeInstanceOf(AuthError);
  });

  it("rejects unknown email", async () => {
    await expect(
      authenticateUser("ninguem@empresa.com", "changeme123"),
    ).rejects.toBeInstanceOf(AuthError);
  });
});
```

- [ ] **Step 3: Rodar e verificar que falha**

Run: `pnpm --filter @legends/api test src/services/auth-service.test.ts`
Expected: FAIL — não resolve `./auth-service`.

- [ ] **Step 4: Implementar `apps/api/src/services/auth-service.ts`**

```ts
import type { User } from "@prisma/client";
import { prisma } from "../lib/prisma";
import { hashPassword, verifyPassword } from "../lib/password";

export class AuthError extends Error {}

interface RegisterInput {
  name: string;
  email: string;
  password: string;
  position?: string;
  squad?: string;
}

export async function registerUser(input: RegisterInput): Promise<User> {
  const existing = await prisma.user.findUnique({
    where: { email: input.email },
  });
  if (existing) {
    throw new AuthError("E-mail já cadastrado");
  }
  const passwordHash = await hashPassword(input.password);
  return prisma.user.create({
    data: {
      name: input.name,
      email: input.email,
      passwordHash,
      position: input.position,
      squad: input.squad,
    },
  });
}

export async function authenticateUser(
  email: string,
  password: string,
): Promise<User> {
  const user = await prisma.user.findUnique({ where: { email } });
  if (!user || !user.active) {
    throw new AuthError("Credenciais inválidas");
  }
  const ok = await verifyPassword(password, user.passwordHash);
  if (!ok) {
    throw new AuthError("Credenciais inválidas");
  }
  return user;
}
```

- [ ] **Step 5: Rodar e verificar que passa**

Run: `pnpm --filter @legends/api test src/services/auth-service.test.ts`
Expected: PASS — 5 testes verdes.

- [ ] **Step 6: Commit**

```bash
git add apps/api/src/lib/serialize.ts apps/api/src/services/auth-service.ts apps/api/src/services/auth-service.test.ts
git commit -m "feat(api): add auth service (register/authenticate)"
```

---

## Task 6: Rotas de auth `/auth/register`, `/auth/login`, `/auth/me` (TDD)

**Files:**

- Create: `apps/api/src/routes/auth.ts`
- Test: `apps/api/src/routes/auth.test.ts`
- Modify: `apps/api/src/app.ts`

- [ ] **Step 1: Registrar as rotas de auth em `apps/api/src/app.ts`**

Adicione o import no topo e o `register` antes do `return app`. Arquivo final:

```ts
import Fastify, { type FastifyInstance } from "fastify";
import cors from "@fastify/cors";
import jwt from "@fastify/jwt";
import { authRoutes } from "./routes/auth";

export function buildApp(): FastifyInstance {
  const app = Fastify({ logger: false });

  app.register(cors, { origin: true });
  app.register(jwt, {
    secret: process.env.JWT_SECRET ?? "dev-secret-change-me",
  });

  app.decorate("authenticate", async function (request, reply) {
    try {
      await request.jwtVerify();
    } catch {
      return reply.code(401).send({ message: "Não autorizado" });
    }
  });

  app.get("/health", async () => ({ status: "ok" }));

  app.register(authRoutes, { prefix: "/auth" });

  return app;
}
```

- [ ] **Step 2: Escrever o teste que falha — `apps/api/src/routes/auth.test.ts`**

```ts
import { describe, it, expect } from "vitest";
import { buildApp } from "../app";

async function makeApp() {
  const app = buildApp();
  await app.ready();
  return app;
}

describe("auth routes", () => {
  it("registers a user and returns a token", async () => {
    const app = await makeApp();
    const res = await app.inject({
      method: "POST",
      url: "/auth/register",
      payload: {
        name: "Ana",
        email: "ana@empresa.com",
        password: "changeme123",
      },
    });
    expect(res.statusCode).toBe(201);
    const body = res.json();
    expect(body.token).toBeTruthy();
    expect(body.user.email).toBe("ana@empresa.com");
    expect(body.user).not.toHaveProperty("passwordHash");
    await app.close();
  });

  it("rejects registration with invalid payload", async () => {
    const app = await makeApp();
    const res = await app.inject({
      method: "POST",
      url: "/auth/register",
      payload: { name: "Ana", email: "not-an-email", password: "123" },
    });
    expect(res.statusCode).toBe(400);
    await app.close();
  });

  it("rejects duplicate registration with 409", async () => {
    const app = await makeApp();
    const payload = {
      name: "Ana",
      email: "ana@empresa.com",
      password: "changeme123",
    };
    await app.inject({ method: "POST", url: "/auth/register", payload });
    const res = await app.inject({
      method: "POST",
      url: "/auth/register",
      payload,
    });
    expect(res.statusCode).toBe(409);
    await app.close();
  });

  it("logs in with correct credentials", async () => {
    const app = await makeApp();
    await app.inject({
      method: "POST",
      url: "/auth/register",
      payload: {
        name: "Ana",
        email: "ana@empresa.com",
        password: "changeme123",
      },
    });
    const res = await app.inject({
      method: "POST",
      url: "/auth/login",
      payload: { email: "ana@empresa.com", password: "changeme123" },
    });
    expect(res.statusCode).toBe(200);
    expect(res.json().token).toBeTruthy();
    await app.close();
  });

  it("rejects login with wrong password (401)", async () => {
    const app = await makeApp();
    await app.inject({
      method: "POST",
      url: "/auth/register",
      payload: {
        name: "Ana",
        email: "ana@empresa.com",
        password: "changeme123",
      },
    });
    const res = await app.inject({
      method: "POST",
      url: "/auth/login",
      payload: { email: "ana@empresa.com", password: "wrong-password" },
    });
    expect(res.statusCode).toBe(401);
    await app.close();
  });

  it("returns the current user from /auth/me with a valid token", async () => {
    const app = await makeApp();
    const reg = await app.inject({
      method: "POST",
      url: "/auth/register",
      payload: {
        name: "Ana",
        email: "ana@empresa.com",
        password: "changeme123",
      },
    });
    const { token } = reg.json();
    const res = await app.inject({
      method: "GET",
      url: "/auth/me",
      headers: { authorization: `Bearer ${token}` },
    });
    expect(res.statusCode).toBe(200);
    expect(res.json().user.email).toBe("ana@empresa.com");
    await app.close();
  });

  it("rejects /auth/me without a token (401)", async () => {
    const app = await makeApp();
    const res = await app.inject({ method: "GET", url: "/auth/me" });
    expect(res.statusCode).toBe(401);
    await app.close();
  });
});
```

- [ ] **Step 3: Rodar e verificar que falha**

Run: `pnpm --filter @legends/api test src/routes/auth.test.ts`
Expected: FAIL — não resolve `./routes/auth` (import em `app.ts`).

- [ ] **Step 4: Implementar `apps/api/src/routes/auth.ts`**

```ts
import type { FastifyInstance } from "fastify";
import { z } from "zod";
import {
  AuthError,
  authenticateUser,
  registerUser,
} from "../services/auth-service";
import { toPublicUser } from "../lib/serialize";
import { prisma } from "../lib/prisma";

const registerSchema = z.object({
  name: z.string().min(1),
  email: z.string().email(),
  password: z.string().min(8),
  position: z.string().optional(),
  squad: z.string().optional(),
});

const loginSchema = z.object({
  email: z.string().email(),
  password: z.string().min(1),
});

export async function authRoutes(app: FastifyInstance) {
  app.post("/register", async (request, reply) => {
    const parsed = registerSchema.safeParse(request.body);
    if (!parsed.success) {
      return reply
        .code(400)
        .send({ message: "Dados inválidos", issues: parsed.error.flatten() });
    }
    try {
      const user = await registerUser(parsed.data);
      const token = app.jwt.sign({ sub: user.id, role: user.role });
      return reply.code(201).send({ token, user: toPublicUser(user) });
    } catch (err) {
      if (err instanceof AuthError) {
        return reply.code(409).send({ message: err.message });
      }
      throw err;
    }
  });

  app.post("/login", async (request, reply) => {
    const parsed = loginSchema.safeParse(request.body);
    if (!parsed.success) {
      return reply.code(400).send({ message: "Dados inválidos" });
    }
    try {
      const user = await authenticateUser(
        parsed.data.email,
        parsed.data.password,
      );
      const token = app.jwt.sign({ sub: user.id, role: user.role });
      return reply.send({ token, user: toPublicUser(user) });
    } catch (err) {
      if (err instanceof AuthError) {
        return reply.code(401).send({ message: err.message });
      }
      throw err;
    }
  });

  app.get("/me", { onRequest: [app.authenticate] }, async (request, reply) => {
    const user = await prisma.user.findUnique({
      where: { id: request.user.sub },
    });
    if (!user) {
      return reply.code(404).send({ message: "Usuário não encontrado" });
    }
    return reply.send({ user: toPublicUser(user) });
  });
}
```

- [ ] **Step 5: Rodar e verificar que passa**

Run: `pnpm --filter @legends/api test src/routes/auth.test.ts`
Expected: PASS — 7 testes verdes.

- [ ] **Step 6: Commit**

```bash
git add apps/api/src/routes/auth.ts apps/api/src/routes/auth.test.ts apps/api/src/app.ts
git commit -m "feat(api): add auth routes (register, login, me)"
```

---

## Task 7: Rota `GET /users` (TDD)

**Files:**

- Create: `apps/api/src/routes/users.ts`
- Test: `apps/api/src/routes/users.test.ts`
- Modify: `apps/api/src/app.ts`

- [ ] **Step 1: Registrar a rota de users em `apps/api/src/app.ts`**

Adicione o import e o `register`. Arquivo final:

```ts
import Fastify, { type FastifyInstance } from "fastify";
import cors from "@fastify/cors";
import jwt from "@fastify/jwt";
import { authRoutes } from "./routes/auth";
import { userRoutes } from "./routes/users";

export function buildApp(): FastifyInstance {
  const app = Fastify({ logger: false });

  app.register(cors, { origin: true });
  app.register(jwt, {
    secret: process.env.JWT_SECRET ?? "dev-secret-change-me",
  });

  app.decorate("authenticate", async function (request, reply) {
    try {
      await request.jwtVerify();
    } catch {
      return reply.code(401).send({ message: "Não autorizado" });
    }
  });

  app.get("/health", async () => ({ status: "ok" }));

  app.register(authRoutes, { prefix: "/auth" });
  app.register(userRoutes);

  return app;
}
```

- [ ] **Step 2: Escrever o teste que falha — `apps/api/src/routes/users.test.ts`**

```ts
import { describe, it, expect } from "vitest";
import { buildApp } from "../app";

async function registerAndToken(
  app: ReturnType<typeof buildApp>,
  email: string,
) {
  const res = await app.inject({
    method: "POST",
    url: "/auth/register",
    payload: { name: email.split("@")[0], email, password: "changeme123" },
  });
  return res.json().token as string;
}

describe("GET /users", () => {
  it("lists active users excluding the requester", async () => {
    const app = buildApp();
    await app.ready();
    const token = await registerAndToken(app, "ana@empresa.com");
    await registerAndToken(app, "bruno@empresa.com");
    await registerAndToken(app, "carla@empresa.com");

    const res = await app.inject({
      method: "GET",
      url: "/users",
      headers: { authorization: `Bearer ${token}` },
    });
    expect(res.statusCode).toBe(200);
    const emails = res.json().users.map((u: { email: string }) => u.email);
    expect(emails).toContain("bruno@empresa.com");
    expect(emails).toContain("carla@empresa.com");
    expect(emails).not.toContain("ana@empresa.com");
    await app.close();
  });

  it("rejects unauthenticated requests (401)", async () => {
    const app = buildApp();
    await app.ready();
    const res = await app.inject({ method: "GET", url: "/users" });
    expect(res.statusCode).toBe(401);
    await app.close();
  });
});
```

- [ ] **Step 3: Rodar e verificar que falha**

Run: `pnpm --filter @legends/api test src/routes/users.test.ts`
Expected: FAIL — não resolve `./routes/users`.

- [ ] **Step 4: Implementar `apps/api/src/routes/users.ts`**

```ts
import type { FastifyInstance } from "fastify";
import { prisma } from "../lib/prisma";
import { toPublicUser } from "../lib/serialize";

export async function userRoutes(app: FastifyInstance) {
  app.get(
    "/users",
    { onRequest: [app.authenticate] },
    async (request, reply) => {
      const users = await prisma.user.findMany({
        where: { active: true, id: { not: request.user.sub } },
        orderBy: { name: "asc" },
      });
      return reply.send({ users: users.map(toPublicUser) });
    },
  );
}
```

- [ ] **Step 5: Rodar e verificar que passa**

Run: `pnpm --filter @legends/api test src/routes/users.test.ts`
Expected: PASS — 2 testes verdes.

- [ ] **Step 6: Rodar a suíte completa do backend**

Run: `pnpm --filter @legends/api test`
Expected: PASS — health + password + auth-service + auth routes + users routes, todos verdes.

- [ ] **Step 7: Commit**

```bash
git add apps/api/src/routes/users.ts apps/api/src/routes/users.test.ts apps/api/src/app.ts
git commit -m "feat(api): add GET /users (eligible developers) route"
```

---

## Task 8: Frontend — deps, cliente de API e proxy

**Files:**

- Modify: `apps/web/package.json` (deps `react-router-dom`, `@tanstack/react-query`)
- Modify: `apps/web/vite.config.ts` (proxy `/api` com rewrite)
- Create: `apps/web/src/lib/api.ts`

- [ ] **Step 1: Adicionar dependências em `apps/web/package.json`**

Bloco `dependencies` final:

```json
  "dependencies": {
    "@destaque/shared": "workspace:*",
    "@legends/shared": "workspace:*",
    "@tanstack/react-query": "^5.51.23",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.1"
  }
```

> ATENÇÃO: substitua a linha `"@destaque/shared"` se existir — o nome correto é `@legends/shared`. O bloco final deve conter **apenas** `@legends/shared` (sem `@destaque/shared`). Mantenha a ordenação alfabética acima.

- [ ] **Step 2: Instalar dependências**

Run: `pnpm install`
Expected: instala `react-router-dom` e `@tanstack/react-query` em `@legends/web`.

- [ ] **Step 3: Atualizar o proxy em `apps/web/vite.config.ts`** (reescreve `/api` removendo o prefixo)

```ts
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      "/api": {
        target: "http://localhost:3333",
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, ""),
      },
    },
  },
  test: {
    environment: "jsdom",
    globals: true,
    setupFiles: "./src/test/setup.ts",
  },
});
```

- [ ] **Step 4: Criar `apps/web/src/lib/api.ts`**

```ts
const TOKEN_KEY = "legends.token";

export function getToken(): string | null {
  return localStorage.getItem(TOKEN_KEY);
}

export function setToken(token: string): void {
  localStorage.setItem(TOKEN_KEY, token);
}

export function clearToken(): void {
  localStorage.removeItem(TOKEN_KEY);
}

export class ApiError extends Error {
  constructor(
    public status: number,
    message: string,
  ) {
    super(message);
    this.name = "ApiError";
  }
}

export async function apiFetch<T>(
  path: string,
  options: RequestInit = {},
): Promise<T> {
  const token = getToken();
  const res = await fetch(`/api${path}`, {
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...(token ? { Authorization: `Bearer ${token}` } : {}),
      ...options.headers,
    },
  });

  if (!res.ok) {
    let message = "Erro inesperado";
    try {
      const body = (await res.json()) as { message?: string };
      if (body.message) message = body.message;
    } catch {
      // resposta sem corpo JSON
    }
    throw new ApiError(res.status, message);
  }

  return (await res.json()) as T;
}
```

- [ ] **Step 5: Verificar build de tipos**

Run: `pnpm --filter @legends/web test`
Expected: PASS — o teste atual (`App.test.tsx`) continua verde (nada o usa ainda).

- [ ] **Step 6: Commit**

```bash
git add apps/web/package.json apps/web/vite.config.ts apps/web/src/lib/api.ts pnpm-lock.yaml
git commit -m "feat(web): add api client, router/query deps and api proxy rewrite"
```

---

## Task 9: Frontend — `AuthContext` e `ProtectedRoute`

**Files:**

- Create: `apps/web/src/auth/AuthContext.tsx`
- Create: `apps/web/src/auth/ProtectedRoute.tsx`

- [ ] **Step 1: Criar `apps/web/src/auth/AuthContext.tsx`**

```tsx
import {
  createContext,
  useContext,
  useEffect,
  useState,
  type ReactNode,
} from "react";
import type { AuthResponse, PublicUser } from "@legends/shared";
import { apiFetch, clearToken, getToken, setToken } from "../lib/api";

interface AuthContextValue {
  user: PublicUser | null;
  loading: boolean;
  login: (email: string, password: string) => Promise<void>;
  logout: () => void;
}

const AuthContext = createContext<AuthContextValue | undefined>(undefined);

export function AuthProvider({ children }: { children: ReactNode }) {
  const [user, setUser] = useState<PublicUser | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    const token = getToken();
    if (!token) {
      setLoading(false);
      return;
    }
    apiFetch<{ user: PublicUser }>("/auth/me")
      .then((res) => setUser(res.user))
      .catch(() => clearToken())
      .finally(() => setLoading(false));
  }, []);

  async function login(email: string, password: string) {
    const res = await apiFetch<AuthResponse>("/auth/login", {
      method: "POST",
      body: JSON.stringify({ email, password }),
    });
    setToken(res.token);
    setUser(res.user);
  }

  function logout() {
    clearToken();
    setUser(null);
  }

  return (
    <AuthContext.Provider value={{ user, loading, login, logout }}>
      {children}
    </AuthContext.Provider>
  );
}

export function useAuth(): AuthContextValue {
  const ctx = useContext(AuthContext);
  if (!ctx) {
    throw new Error("useAuth deve ser usado dentro de AuthProvider");
  }
  return ctx;
}
```

- [ ] **Step 2: Criar `apps/web/src/auth/ProtectedRoute.tsx`**

```tsx
import type { ReactNode } from "react";
import { Navigate } from "react-router-dom";
import { useAuth } from "./AuthContext";

export function ProtectedRoute({ children }: { children: ReactNode }) {
  const { user, loading } = useAuth();
  if (loading) {
    return <p className="p-8 font-mono text-secondary">Carregando…</p>;
  }
  if (!user) {
    return <Navigate to="/login" replace />;
  }
  return <>{children}</>;
}
```

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/auth
git commit -m "feat(web): add auth context and protected route"
```

---

## Task 10: Frontend — página de Login (TDD)

**Files:**

- Create: `apps/web/src/pages/LoginPage.tsx`
- Test: `apps/web/src/pages/LoginPage.test.tsx`

- [ ] **Step 1: Escrever o teste que falha — `apps/web/src/pages/LoginPage.test.tsx`**

```tsx
import { render, screen, fireEvent, waitFor } from "@testing-library/react";
import { MemoryRouter } from "react-router-dom";
import { vi } from "vitest";
import { LoginPage } from "./LoginPage";
import { ApiError } from "../lib/api";

const loginMock = vi.fn();

vi.mock("../auth/AuthContext", () => ({
  useAuth: () => ({
    login: loginMock,
    user: null,
    loading: false,
    logout: vi.fn(),
  }),
}));

function renderLogin() {
  return render(
    <MemoryRouter>
      <LoginPage />
    </MemoryRouter>,
  );
}

describe("LoginPage", () => {
  beforeEach(() => loginMock.mockReset());

  it("submits the typed credentials", async () => {
    loginMock.mockResolvedValue(undefined);
    renderLogin();
    fireEvent.change(screen.getByLabelText(/e-mail/i), {
      target: { value: "ana@empresa.com" },
    });
    fireEvent.change(screen.getByLabelText(/senha/i), {
      target: { value: "changeme123" },
    });
    fireEvent.click(screen.getByRole("button", { name: /entrar/i }));
    await waitFor(() =>
      expect(loginMock).toHaveBeenCalledWith("ana@empresa.com", "changeme123"),
    );
  });

  it("shows an error message when login fails", async () => {
    loginMock.mockRejectedValue(new ApiError(401, "Credenciais inválidas"));
    renderLogin();
    fireEvent.change(screen.getByLabelText(/e-mail/i), {
      target: { value: "x@y.com" },
    });
    fireEvent.change(screen.getByLabelText(/senha/i), {
      target: { value: "wrong-password" },
    });
    fireEvent.click(screen.getByRole("button", { name: /entrar/i }));
    expect(await screen.findByRole("alert")).toHaveTextContent(
      /credenciais inválidas/i,
    );
  });
});
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `pnpm --filter @legends/web test src/pages/LoginPage.test.tsx`
Expected: FAIL — não resolve `./LoginPage`.

- [ ] **Step 3: Implementar `apps/web/src/pages/LoginPage.tsx`**

```tsx
import { useState, type FormEvent } from "react";
import { useNavigate } from "react-router-dom";
import { useAuth } from "../auth/AuthContext";
import { ApiError } from "../lib/api";

export function LoginPage() {
  const { login } = useAuth();
  const navigate = useNavigate();
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [error, setError] = useState<string | null>(null);
  const [submitting, setSubmitting] = useState(false);

  async function handleSubmit(event: FormEvent) {
    event.preventDefault();
    setError(null);
    setSubmitting(true);
    try {
      await login(email, password);
      navigate("/");
    } catch (err) {
      setError(err instanceof ApiError ? err.message : "Erro ao entrar");
    } finally {
      setSubmitting(false);
    }
  }

  return (
    <main className="min-h-screen flex items-center justify-center p-4">
      <form
        onSubmit={handleSubmit}
        className="w-full max-w-sm bg-surface rounded-lg p-8 flex flex-col gap-4"
      >
        <h1 className="text-2xl font-extrabold tracking-tight text-center">
          Engineering <span className="text-primary">Legends</span>
        </h1>
        <label className="flex flex-col gap-1 font-mono text-xs text-secondary">
          E-mail
          <input
            type="email"
            value={email}
            onChange={(event) => setEmail(event.target.value)}
            required
            className="rounded bg-bg border border-slate-700 px-3 py-2 font-sans text-base text-slate-100 focus:border-primary focus:outline-none"
          />
        </label>
        <label className="flex flex-col gap-1 font-mono text-xs text-secondary">
          Senha
          <input
            type="password"
            value={password}
            onChange={(event) => setPassword(event.target.value)}
            required
            className="rounded bg-bg border border-slate-700 px-3 py-2 font-sans text-base text-slate-100 focus:border-primary focus:outline-none"
          />
        </label>
        {error && (
          <p role="alert" className="text-sm text-red-400">
            {error}
          </p>
        )}
        <button
          type="submit"
          disabled={submitting}
          className="rounded bg-primary text-primary-fg font-semibold py-2 hover:opacity-90 disabled:opacity-50"
        >
          {submitting ? "Entrando…" : "Entrar"}
        </button>
      </form>
    </main>
  );
}
```

- [ ] **Step 4: Rodar e verificar que passa**

Run: `pnpm --filter @legends/web test src/pages/LoginPage.test.tsx`
Expected: PASS — 2 testes verdes.

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/pages/LoginPage.tsx apps/web/src/pages/LoginPage.test.tsx
git commit -m "feat(web): add login page"
```

---

## Task 11: Frontend — router, página de Time e wiring final

**Files:**

- Create: `apps/web/src/pages/TeamPage.tsx`
- Modify: `apps/web/src/App.tsx`
- Modify: `apps/web/src/App.test.tsx`

- [ ] **Step 1: Criar `apps/web/src/pages/TeamPage.tsx`**

```tsx
import { useQuery } from "@tanstack/react-query";
import type { PublicUser } from "@legends/shared";
import { apiFetch } from "../lib/api";
import { useAuth } from "../auth/AuthContext";

export function TeamPage() {
  const { user, logout } = useAuth();
  const { data, isLoading, isError } = useQuery({
    queryKey: ["users"],
    queryFn: () => apiFetch<{ users: PublicUser[] }>("/users"),
  });

  return (
    <main className="min-h-screen p-8 flex flex-col gap-6">
      <header className="flex items-center justify-between">
        <h1 className="text-3xl font-extrabold tracking-tight">
          Engineering <span className="text-primary">Legends</span>
        </h1>
        <div className="flex items-center gap-4">
          <span className="font-mono text-xs text-secondary">{user?.name}</span>
          <button
            onClick={logout}
            className="text-sm text-slate-400 hover:text-slate-100"
          >
            Sair
          </button>
        </div>
      </header>
      <section>
        <h2 className="font-mono text-xs uppercase tracking-widest text-secondary mb-3">
          Time
        </h2>
        {isLoading && <p className="text-slate-400">Carregando time…</p>}
        {isError && <p className="text-red-400">Erro ao carregar o time.</p>}
        <ul className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">
          {data?.users.map((member) => (
            <li key={member.id} className="bg-surface rounded-lg p-4">
              <p className="font-semibold">{member.name}</p>
              <p className="font-mono text-xs text-secondary">
                {member.position ?? "—"} · {member.squad ?? "—"}
              </p>
            </li>
          ))}
        </ul>
      </section>
    </main>
  );
}
```

- [ ] **Step 2: Substituir `apps/web/src/App.tsx`** (providers + router)

```tsx
import { BrowserRouter, Navigate, Route, Routes } from "react-router-dom";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { AuthProvider } from "./auth/AuthContext";
import { ProtectedRoute } from "./auth/ProtectedRoute";
import { LoginPage } from "./pages/LoginPage";
import { TeamPage } from "./pages/TeamPage";

const queryClient = new QueryClient();

export function App() {
  return (
    <QueryClientProvider client={queryClient}>
      <AuthProvider>
        <BrowserRouter>
          <Routes>
            <Route path="/login" element={<LoginPage />} />
            <Route
              path="/"
              element={
                <ProtectedRoute>
                  <TeamPage />
                </ProtectedRoute>
              }
            />
            <Route path="*" element={<Navigate to="/" replace />} />
          </Routes>
        </BrowserRouter>
      </AuthProvider>
    </QueryClientProvider>
  );
}
```

- [ ] **Step 3: Substituir `apps/web/src/App.test.tsx`** (sem token → redireciona para Login, que mostra o heading)

```tsx
import { render, screen } from "@testing-library/react";
import { App } from "./App";

describe("App", () => {
  it("redirects unauthenticated users to the login screen", async () => {
    localStorage.clear();
    render(<App />);
    expect(
      await screen.findByRole("heading", { name: /Legends/i }),
    ).toBeInTheDocument();
    expect(
      await screen.findByRole("button", { name: /entrar/i }),
    ).toBeInTheDocument();
  });
});
```

- [ ] **Step 4: Rodar a suíte completa do frontend**

Run: `pnpm --filter @legends/web test`
Expected: PASS — `App.test.tsx` e `LoginPage.test.tsx` verdes.

- [ ] **Step 5: Verificação manual ponta a ponta**

Run (em terminais separados, ou `pnpm dev` na raiz):

```bash
docker compose up -d
pnpm db:seed
pnpm dev
```

Expected: acessar `http://localhost:5173` redireciona para `/login`. Logar com `ana@empresa.com` / `changeme123` leva à página de Time, listando os outros devs do seed (Bruno, Carla, Diego, Admin) — sem a própria Ana. "Sair" volta para o login. Feche os processos com Ctrl+C ao terminar.

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/pages/TeamPage.tsx apps/web/src/App.tsx apps/web/src/App.test.tsx
git commit -m "feat(web): wire router, auth flow and team page"
```

---

## Self-Review (cobertura vs. requisitos de Auth & Usuários)

- **Login com email/senha + JWT** (PRD §11 Autenticação): Tasks 3–6. ✔
- **Registro com hash de senha + validação:** Tasks 5–6 (Zod, bcrypt, e-mail único → 409). ✔
- **`/me` autenticado:** Task 6. ✔
- **Listagem de devs elegíveis** (PRD §6: "Listagem dos desenvolvedores elegíveis"): Task 7 (`GET /users`, ativos, exclui o próprio usuário). ✔
- **Perfil mínimo no DTO** (foto, nome, cargo, squad, data de entrada — PRD §11): `PublicUser` em Task 1. ✔
- **Sessão no frontend + tela de Login:** Tasks 8–11 (token em `localStorage`, `AuthContext`, rota protegida, página de Login, página de Time). ✔
- **Infra de testes com banco real:** Task 2 (`legends_test`, truncate por teste, execução sequencial). ✔
- **Risco do `@legends/shared` (achado do Plano 1):** mitigado — o front importa de `@legends/shared` **apenas com `import type`** (`AuthContext`, `TeamPage`, `serialize` no back); esses imports são apagados pelo esbuild/Vite/tsup, então não há consumo de valor em runtime e nenhum empacotamento extra é necessário. ✔
- **Type consistency:** `PublicUser`/`AuthResponse` definidos em Task 1 e usados igualmente no back (`toPublicUser`) e front (`AuthContext`, `TeamPage`); rotas retornam `{ token, user }` (login/register) e `{ user }` (`/me`), `{ users }` (`/users`) — consumidos coerentemente. ✔

Fora do escopo deste plano (próximos): votação, ranking, selos, histórico, admin.
